# Archivo de ejemplo de variables de entorno para SUMAQ UYWA
# NO SUBIR ESTE ARCHIVO A GITHUB - está en .gitignore

# === CONFIGURACIÓN GENERAL ===
NODE_ENV=development
PORT=4000

# === BASE DE DATOS ===
# Para desarrollo local (usa tu PostgreSQL local)
DATABASE_URL="postgresql://username:password@localhost:5432/sumaq_uywa_dev"

# Para producción en Render (se configurará automáticamente)
# DATABASE_URL="postgresql://user:password@host:5432/database"

# === AUTENTICACIÓN ===
# Cambia este secret por algo más seguro en producción
JWT_SECRET="tu_jwt_secret_muy_seguro_aqui_123456789"

# === CORS ===
# Para desarrollo
CORS_ORIGIN="http://localhost:5173"

# Para producción en Render
# CORS_ORIGIN="https://tu-frontend.onrender.com"

# === FRONTEND (para desarrollo local) ===
# Crear un archivo .env en la raíz del proyecto con:
# VITE_API_URL=http://localhost:4000/api

# === NOTAS IMPORTANTES ===
# 1. En producción (Render), estas variables se configuran en el dashboard
# 2. Nunca subas las variables reales a GitHub
# 3. Usa valores seguros para JWT_SECRET en producción
# 4. Las URLs deben coincidir entre frontend y backend
